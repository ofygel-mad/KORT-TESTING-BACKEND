node_modules/
dist/
dist-e2e/
coverage/
server/coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
!server/.env.example
*.db
*.db-journal
*.dump

# DB reports — contain raw data including password hashes and tokens, NEVER commit
_database-report/

# Playwright auth states and test artifacts
tests/e2e/.auth/
test-results/
playwright-report/

# Control Plane (R4.3) — a separate, independent git repository living
# physically alongside KORT. Never tracked by the KORT repo.
control-plane/
